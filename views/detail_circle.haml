%h1
- @circle.users.each do |user|
    - if user.id != logged_in_user.id
        .remove-link
            %span
                =h user.name
            - if your_circle @circle
                = link_to "/circle/#{@circle.id}/remove/#{user.id}", "Remove '#{user.name}' from this circle" 
%h1
    TASKS
- @circle.tasks.each do |task|
    = h task.name
    = h task.location
    = h task.due


